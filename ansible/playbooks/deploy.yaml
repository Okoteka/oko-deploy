---
- hosts: Deploy production
  gather_facts: false
  roles:
    - role: ci_guard
    - role: deploy
      vars:
        directory: "{{ app_dir }}"
        user: "{{ ansible_user }}"
        compose_files:
          - src: "{{ playbook_dir }}/../../compose/docker-compose.yaml"
            dest: "docker-compose.yaml"
